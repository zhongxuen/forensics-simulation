# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

@AGENTS.md

**Start with `docs/plan/00-overview.md`.** It holds the decisions, the waves and how to run each plan file's prompts. `docs/plan/99-reference.md` holds the story world, voice rules, tool table and risks that every prompt points to.

## Project status

Plan file 01 (foundation) is built: a Next.js 16 (App Router) + React 19 + TypeScript (strict) + Tailwind CSS 4 app, managed with pnpm 11, with Hacker Simulation's pure engine, terminal, settings, UI kit, app shell, lesson pipeline and quality scripts vendored from `../hacker-simulation` at commit `4c62fe9` (`VENDORED.md` lists every copied path and every change since). Routes: `/` (the pitch, the SIMULATED line, "Open Case 1" and the disclaimers), `/cases` (the chapter's released cases) and `/cases/[slug]` (the case runner), `/sandbox` (the practice kit from `src/content/cases/sandbox.yaml` on the analyst workstation `ir-ws-01`, account `examiner`), `/learn` (the Learning Center: tracks, `/learn/[lessonId]`, `/learn/glossary`, `/learn/commands`), `/settings` (the vendored settings form, key `incident-room:settings`), `/privacy`, and `/styleguide` (dev only).

Prompt 01.2 is done too: `.github/workflows/ci.yml` is adapted from the sibling (and now runs `pnpm evidence:check` and `pnpm case:validate --strict`), `scripts/bundle-baseline.json` is reset for these routes, and `tests/e2e/a11y.spec.ts` runs axe on every route. The Vercel project deploys `main` through its Git integration; nothing in CI deploys.

Plan file 02 (the evidence model) is built, in `src/sim/evidence/` (see its README): the disk, memory, log and evidence-set types with their `zod/mini` schemas, artefact refs (`parseRef` / `formatRef` / `resolveRef`), zoned time from a committed offset table instead of `Intl`, `renderLog` per source and event id, `mountRead` / `mountWrite`, `imageBytes` (the one byte layout every hash is taken of) with pure MD5 / SHA-1 / SHA-256 in `hash/`, and `build`, a fluent test builder for fixtures.

Plan file 03 (the case format and generator) is built. A case is one YAML file in `src/content/cases`, holding the **ground-truth story**; `src/sim/evidence/generate/` plays it and produces the disk, memory and logs, so nobody writes evidence by hand (`src/content/cases/README.md` is the format, `src/features/cases/README.md` the loader and headless play). Authoring is `pnpm case:new <id>`, `pnpm case:validate [id…]` (`--strict` in CI), `pnpm case:play <id>` and `pnpm evidence:build` / `evidence:check`. `_fixture.yaml` and its playthrough are the pipeline's end-to-end test, and `tests/content/` holds the seven groups that keep a case honest — schema, consistency, staleness, determinism, solvability, answer integrity and the world rules — plus `case-failure-modes.test.ts`, which moves a story action an hour and shows each guard catching it.

Plan file 04 (the disk tools) is built, in `src/sim/tools/forensics/` (see its README): `blocker`, `acquire`, `hashsum`, `lsfs`, `inode`, `recover` and `pin`, each with a man page carrying a **Real-world equivalent** section, beginner explainers for every error, and output lines that carry artefact refs. Evidence reaches the terminal as devices under `/dev/evidence` through `src/sim/evidence/session.ts`, each behind a write-blocker that starts on; reading an original with it off calls `mountWrite` and moves the drive's hash, which is Case 1's lesson. The registry test bans real tool names as command names.

Plan file 05 (the workspace UI) is built: `caseRunReducer` + `useCaseRun` + `CaseRunner` (briefing → workspace → report → debrief) in `src/features/cases/`, the pane registry in `workspace-panes.ts` (files 09 and 10 add one line each), the Autopsy-like Evidence Browser in `src/features/evidence-browser/`, and saved case runs in `src/lib/case-storage/` (key `incident-room:cases:v1`, a command log replayed on load, and a calm banner when storage is blocked). `/privacy` has Clear everything, Export and Import.

Plan file 13 (the Learning Center) is built: prompt 13.1 wrote the Foundations track — what forensics is, order of volatility, chain of custody, hashing for evidence — with `src/content/tracks.ts`, the citations in `src/content/references.ts`, and `externalPrerequisites` linking Hacker Simulation's lessons. Prompt 13.2 wrote the Disk track — partitions and file systems, MACB times, deleted versus overwritten, carving — on **generated practice evidence**: `src/content/practice/stories.ts` holds a story per practice drive (Candlewright's TRAIN-07 stick and practice laptop `train-lt-01`, baseline `training-laptop-v1`), `pnpm evidence:build` plays them into `src/content/practice/<id>.evidence.json`, and a practice machine in `mini-terminals.ts` names one in `evidence` (with optional `prepare` commands), which `<MiniTerminal>` attaches under `/dev/evidence` (`src/features/learning/practice/`). The hashing lesson now images TRAIN-07 and runs `hashsum --verify` against its form. Each disk tool's `help.lesson` names its lesson, which `man`, `--help` and `/learn/commands` show. Prompt 13.3 wrote the Memory track — why memory is captured first, processes and parents, network connections, code injection — and the Logs and timelines track — Windows sign-in events, time zones and clocks, super-timelines, writing the report — on a third practice story, `train-lt-02` (Kit's hunt drill on a second practice laptop: a memory image, security and sysmon-lite logs and a drive), attached to the practice machine `ir-ws-drill`. Every forensics tool's manual page now links a lesson (`mem`, `logq`, `timeline` and `pin` included), and each lesson's "In practice" names the case where the player meets it.

Plan file 06 (Case 1, "The clean copy") is built: `src/content/cases/case-01.yaml`, its playthrough and its generated evidence. A laptop arrives with a handover form, and the case is the copy — check the write-blocker, `acquire`, `hashsum --verify` against the form's SHA-256, then find the note left on the desktop and pin its record. Its playthrough takes the wrong turn first (blocker off, a read of the original, MISMATCH, Reset machine), so the lesson is proved on every build. `src/content/cases/chapter.ts` holds the chapter and its case order, and the landing page, "Start here" and the case list all read it. Prompt 06.2 (the first-five-minutes Playwright spec) has not been run.

Prompt 15A.1 (the "Case 1 only" release) is done. Case 1 plays in the browser: `toRunnableCase` (`src/features/cases/loader/runnable.ts`) turns a case file into the runner's `RunnableCase` at build time, the browser's evaluator handles `pinned`, `reported`, `all` and `any`, and the report asks the case's questions and grades them on the debrief (`run/report.ts`: supported, needs evidence, not yet; "n of m findings supported"). `tests/content/case-browser-play.test.ts` holds the browser runner to headless play. `chapter.ts` has a `released` flag per case: `/cases` lists only released ones (Case 1 now), and the landing page's copy, disclaimers and "still being written" line live in `src/content/release.ts`. `tests/e2e/case-01.spec.ts` plays Case 1 keyboard-only on a desktop and a 360 px phone with axe on every screen. `pnpm perf:vitals` is vendored, the `src/sim` coverage gate is back at Hacker Simulation's levels, and `docs/runbook.md` holds the Vercel settings, rollback, the mentor's firewall rule and the playtest kit, with the owner steps still to do.

Plan file 10 (the Case Board, report grading and Chain of Custody) is built. The Board is `src/features/case-board/` (the pane, registered with one line in `workspace-panes.ts`): cards grouped by source or sorted by time, notes, remove with Undo, and "Show in terminal", "Show in Evidence Browser" and "Show in timeline" through `workstation.show(pane, ref)` and a `reveal` prop. `src/features/cases/grading/` is the one grader (`gradeReport`): an answer is supported only when it is right **and** a pin it cites proves it, so every report question has a Supporting evidence picker and a playthrough's `report` step takes `cite:`. `src/features/cases/custody/` is `custodyLog(events, marks)`, a pure selector over the engine's events (plus saved marks for view pins and report submissions), shown on the Objectives tab's Chain of custody sub-tab and on the debrief, with a plain-text export stamped SIMULATED; its `hashed-before-analysing` rule is a new `custody` objective check, which case-01's "Fingerprint First" bonus uses. The save gained `citations`, `pinNotes` and `marks`, each defaulting to empty, so older saves still load. The report and debrief screens live in `src/features/cases/components/report/` and `debrief/`; the debrief's "Change your report" goes straight back to the report, as often as the player likes.

Plan file 09 (the super-timeline) is built. Prompt 09.1 wrote `buildTimeline` (`src/sim/evidence/timeline.ts`: every moment from the disks' MACB times, memory and logs, in one stable order) and the `timeline` tool with `--around`. Prompt 09.2 wrote the Timeline view, `src/features/timeline/` (see its README), registered as the Timeline pane: tracks per source on a canvas with zoom (whole range, hours, minutes, seconds) and a brush on a whole-case strip, cross-highlighting around the chosen moment, details with Pin, the keyboard map, a table view sharing one reducer (and so one selection), the zone banner and reduced motion. A drive's times reach it only through `workstation.browse`, like the Evidence Browser. 5,000 moments hold 60 fps, measured by `tests/e2e/timeline-performance.spec.ts` on the dev-only `/timeline-bench`.

Plan file 08 (the memory tools) is built: `mem`, in `src/sim/tools/forensics/mem/` (see its README), with a subcommand per question (`info`, `ps`, `psscan`, `pstree`, `netscan`, `cmdline`, `malfind`) against a memory image in the evidence set, every line carrying a `mem:` ref; they emit `memory.listed`, `memory.scanned` and `memory.inspected`. The Evidence Browser gained a Processes tab (`src/features/evidence-browser/memory/`), the `psscan` list with an "In active list?" column. `mem strings` still points at `strings`, which reads memory images.

Plan file 14 (the mentor) is built: Noor Halvorsen, vendored from `../hacker-simulation/src/features/mentor/` and adapted to cases (`VENDORED.md`), in `src/features/mentor/` (see its README) behind `POST /api/mentor/{hint,explain,review}`. She personalises the **authored** hint tiers, explains what is on the player's screen, and looks back at a finished case on the debrief, leading with what they did well and saying how the order of their chain of custody read. **The game is complete without her**: with no `ANTHROPIC_API_KEY`, the `MENTOR_DISABLED=1` kill switch, a rate limit, a model failure or a rejected answer, every hint is the authored text verbatim and nothing surfaces as an error. The safety design is the sibling's, made structural: `toMentorCase` (`case-view.ts`) projects a case down to its objectives and their hints, so no handler or prompt builder is even _given_ a type carrying a report answer, an accepted ref, an objective's `check` or `success` line, or the story's ground truth; every prompt also carries the **forensics rule** (never state an answer, a timestamp from the answer key, or which pin is accepted evidence). "Explain this" works on four surfaces — a terminal line, an Evidence Browser row, a timeline entry and a board card — and sends the rendered line plus, for terminal output, the tool's own man page, never the evidence set. The SDK is imported from one `server-only` module, so `pnpm security:bundle` proves no key, address or SDK reaches a browser. `tests/mentor/injection.test.ts` runs 21 injection attempts (the sibling's 16 plus five new, including a hostile file name the **generator** plants in the evidence and a pin note carrying instructions), `tests/mentor/answer-key.test.ts` proves no prompt carries a character of any case's answer key, and `tests/integration/mentor-routes.test.ts` drives the real routes with only the SDK mocked.

Plan file 07 (carving, strings and log queries) is built: `carve`, `strings` and `logq` in `src/sim/tools/forensics/`, with the signature table and its specification citations in `src/sim/evidence/magic.ts`. `carve` lists each object in unallocated space with its offset, type, size and `disk:<id>:carve/<offset>` ref, and marks one whose end was written over as partial; `strings` reads an image, a carve or `mft` ref, a memory image (with the owning pid's ref) or a workstation file; `logq` filters by source, event id, time and `--where`, and `--count-by` shows a burst in one table. They emit `evidence.carved`, `evidence.searched` and `logs.queried`. Refs now survive a pipe: the shell passes each line's ref along (`ToolContext.stdinRefs`), `grep`, `head` and `tail` keep it, and `pin` after a pipeline points at what reached the screen (`VENDORED.md`). Tests include the carving differential test and the golden transcript `golden/carve-strings-logq.txt`.

Plan file 11 (Case 2, "The deleted invoice") is built: `src/content/cases/case-02.yaml`, its playthrough and its evidence. The bookkeeper's laptop `qf-lt-07`: two invoices recover, a long statement was partly overwritten by the zip and only carves as a partial PDF, a remote sign-in (type 10) from the office laptop deleted them, and the price lists left through the firewall to `203.0.113.80`. The owner's message and the door log are in British Summer Time; the playthrough compares them with the UTC logs unconverted first, then converts, and the evidence clears the bookkeeper. It added `documents` (paperwork in the case folder), `feedback` on a choice question (a choice beat: a cast line on the debrief, then the choice again), `keep` on `overwrite-clusters`, `weekends: false` on noise, and carve patterns that match what the carver finds (`disk:<id>:carve/*text*`). Released by prompt 15B.1.

Plan file 12 (Case 3, "Something is still running") is built: `src/content/cases/case-03.yaml`, its playthrough and its evidence. Quillfen's dispatch server `qf-srv-01` has called `203.0.113.47` once a minute since Sunday night; the case hands over **only a memory capture and logs** (the server stays running until it is rebuilt), so headless play now attaches evidence whenever a case has any, not only a drive. 312 refused sign-ins from `198.51.100.23`, a remote sign-in as `dispatch-admin`, `svc-update` made and put in Administrators, and an `svchost.exe` (pid 4376) in `C:\ProgramData`, started by `explorer.exe`, unlinked, with an MZ region; the dispatch program's runtime-compiler region is the bonus, the 60-second interval the secret. Both choice beats (pull the plug, connect to the address) are report questions with `feedback`, like Case 2's. The chapter's closing line (`chapter.ts`, `closing` and `upNext`) shows on the last case's debrief (`components/debrief/chapter-closing.tsx`), linking Hacker Simulation's Chapter 2.

Prompt 15B.1 (the full launch) is done, on `learning-13.2` after merging 11.1 and 14.1 and building 12.1. All three cases are released (`chapter.ts`), and the landing page says how long each takes instead of "still being written" (`CHAPTER_LENGTH` in `src/content/release.ts`). A case's evidence reaches the browser only through a line in `src/features/cases/run/evidence.ts` (Case 2 had been missing its), and `tests/content/case-chapter.test.ts` now fails for a playable chapter case without one. The **sandbox** is `src/content/cases/sandbox.yaml`: a story with no goals and its own `SandboxSchema` (no client, objectives or report; the case catalog skips it), sharing the case schema's machine and story checks (`checkWorld`). `pnpm evidence:build` plays it into `src/content/sandbox/evidence.json`, `src/features/cases/loader/sandbox.ts` builds its workstation folder (`/home/examiner/cases/sandbox`, through `workstationWith`, which cases use too), and `/sandbox` attaches the evidence on demand with a banner, "Try this first" and the cheat sheet led by Investigate. `tests/content/sandbox.test.ts` holds it current, deterministic and complete, and runs every command it suggests. `tests/e2e/case-02.spec.ts` and `case-03.spec.ts` play Cases 2 and 3 keyboard-only on a desktop and a 360 px phone with axe on every screen (shared steps in `tests/e2e/keyboard.ts`), and `a11y.spec.ts` checks the sandbox terminal in each colour theme. `AppShell` puts page content in its own `<Suspense>` boundary, for INP. `perf:vitals` is still over the INP budget on `/cases` and `/cases/case-01`; `docs/runbook.md` records what was found and lists the owner steps (merge, smoke test, screen reader pass, playtests, INP on a quiet machine, the mentor's firewall rule).

`docs/UIUX.md` is the UI critique and its prompts (UX.1–UX.9). Its "Lamplight" theme (candle amber on ink navy) is in `src/styles/tokens.css`, and prompt UX.1 finished the design system: the type roles as `@utility` classes in `src/styles/typography.css` (`type-display` … `type-data`), a `letter` Card variant, an `evidence-tag` Badge tone, `ObjectiveTick`'s `current` status, `SimulatedBadge`'s `tape` look and `compact` size (once per view: the shell's top bar plus any terminal), the `Menu` and `SplitPane` primitives, `Skeleton`, `Stamp`, the candle mark in place of the `>▌` logo, and UIUX §5's primitive motion (`animate-badge-pop`, `-line-flash`, `-stamp`, `-sweep`, `-shimmer`, `fx-slide` for the tab underline), all on `/styleguide` in both motion modes. Prompt UX.2 reworked the shell, landing and case list: "Start here" is a raised card that follows the runs saved in this browser (`nextStepFor` in `src/lib/next-step-from-runs.ts`: "Continue Case 1 · 1 of 5") and hides inside a case, the top bar shows a breadcrumb (`Cases / The clean copy`, titles from the `(app)` layout), and inside a case run the sidebar starts on the rail (`data-sidebar-view` on the shell, read by the `rail:` variant) without touching the saved setting. The landing page has an eyebrow and H1, one button, the loop strip (`src/app/(marketing)/loop-strip.tsx`, `animate-light-up`), "What you'll do in Case 1" and a quieter disclaimer block, all its copy in `src/content/release.ts`. `/cases` shows each case's number, time and state from its saved run (`caseSummaries` and `caseState` in `run/catalog.ts`), with practice in its own row. Prompt UX.3 reworked the briefing and workspace: the briefing's written permission is a `letter` Card with its scope as ✓/✕ items (`briefing-scope.ts`) and Start case sits in a sticky bar with the time and "Hints are free"; the workspace fills the viewport below the top bar (no page scroll), with a compact header (title, progress ring, Ask Noor, and a "⋯ Case" `Menu` holding Start the case again), the **Now strip** (`now-strip.tsx`: the current objective, Why on demand, Show a hint, and the newest team message with "n earlier messages" opening the chat), then from 1024 px the terminal and the panes in a `SplitPane` with a **Focus pane** toggle that turns the terminal into a drawer (Esc closes it), and below 1024 px one view at a time with a bottom tab bar that follows the visual viewport, so the prompt stays above an on-screen keyboard. Tabs run Objectives · Evidence · Timeline · Board (`PANE_ORDER`), with the Board's pin count and a dot for news; the checklist expands the current objective and collapses done ones. Prompt UX.4 reworked the terminal: in a case the **Try:** chips are the current objective's tools (`objectiveSuggestions` in `src/features/cases/run/suggestions.ts`, passed as the terminal's new `suggestions` prop; a part that already worked drops out), "What just happened?" and "Explain this" sit at the end of each output block, a hash gets its own line with a Copy button, each block has a faint left rule and a successful `pin` flashes, and Copy transcript and Reset machine live in a "More" menu beside Help. Prompt UX.5 reworked the three panes, each laid out for the room its pane has (`useElementWidth` in `src/hooks/`) rather than the screen: below 900 px the Evidence Browser drills in (drives and folders → records → one record, with Back and a breadcrumb) and from 900 px (Focus pane) shows Autopsy's three columns; its filters appear once a drive is open, in a bar with the time window folded away, and before then an empty state offers the one safe drive to open. The Timeline folds its filters into a bar beside the terminal and puts them in a column beside the tracks with Focus pane, leads with the zone banner as the new `info` Callout, and builds in a deferred render with skeleton tracks. Board cards are evidence tags (source icon and word, the ref on the `evidence-tag` badge, the time in mono), rise in on arrival and fade out on Remove (`animate-fade-out`) with an Undo toast. The §2.5 freeze did not reproduce: `tests/e2e/timeline-performance.spec.ts` traces Timeline ⇄ Board after Case 2's first commands and finds no long task (144–164 ms at 4× CPU throttle), numbers in `src/features/timeline/README.md`. Prompt UX.6 reworked the report and debrief (presentation only; `gradeReport` is untouched): the report's questions are numbered cards, each answer's Supporting evidence is a set of pin cards (a native checkbox inside each, with the source word, the ref as an evidence tag, the title, time and note), and a sticky bar holds Submit report beside the progress line (`reportProgress`: "2 of 3 answered, 1 with evidence", what's filled in, never whether it's right). The debrief leads with the `Stamp` and "n of m findings supported", each finding's chip popping in 80 ms after the last (`chipDelay`, scaled by `--motion-scale`), then the chain of custody and its download, then the objectives in brief; its one primary button is **Next case** (`nextCaseFor`, the chapter's next released case) or Back to your cases, with Change your report taking that place while a finding isn't supported, Back to the workspace secondary, and Start the case again in a "⋯ Case" `Menu`. Noor's "look back" button is secondary for the same reason. Prompt UX.7 reworked the Learning Center, sandbox, settings and What we store (layout only; no lesson content or stored data changed): the type roles throughout, lesson cards as the heaviest surface, prerequisite notes ("Best read first") as a left rule with no box, and the level as a borderless chip (`LevelLabel`); every lesson ends with one **Try it in Case N** button, from the first `/cases/<id>` link in its "In practice" section (`inPracticeCaseId`, released cases only, held by `tests/unit/lesson-content.test.ts`). The sandbox already had its evidence (15B.1), so instead of the "Evidence arrives…" callout its aside gains "Want a guide?", the first lesson per track whose practice terminal carries evidence.

## What this project is

Candlewright: Incident Room is a story game where the player joins Candlewright Security's blue team after a made-up break-in at a made-up client (Quillfen Freight). They examine a disk image, a memory dump and a set of logs in a simulated terminal and three investigator views, pin findings to a case board, and close the case with a report where every answer points at evidence. It shares a world, cast and engine with Hacker Simulation, and fills the portfolio's Autopsy, Volatility and FTK Imager gap.

## Aims (docs/plan/00-overview.md §2)

1. **Beginner-first.** Assume zero knowledge. Define every term the first time it appears on a screen. A first win within 2 minutes of landing, with no sign-up.
2. **Safe and ethical.** Blue team only. Every case has a client who asked and signed. No hacking back, even at the attacker. Nobody is targeted as a person.
3. **Accurate, and honest about limits.** A pure, deterministic engine. Evidence is generated from a written story and tested for consistency and solvability. Tool output is realistic but never copied.
4. **No dark patterns.** Hints and the mentor are free and never lower anything. No timers that gate play, no lives, no streaks, no leaderboards.
5. **Quality you can see.** axe on every route, keyboard-only play, reduced motion, 200 KB initial JS per page, Vitest + Playwright, CI on every push.
6. **Portfolio proof.** Something a visitor can play in five minutes.

The quick test for any screen: _would a complete beginner understand this and want to keep going?_ Voice rules and banned words are in `docs/plan/99-reference.md`.

## Commands

Use pnpm (pinned via `packageManager` in `package.json`).

- `pnpm install` — install dependencies
- `pnpm dev` — dev server at http://localhost:3000
- `pnpm build` — production build; also type-checks. `pnpm start` serves it
- `pnpm lint` — ESLint, including the module-boundary rules
- `pnpm typecheck` — `next typegen`, then `tsc --noEmit` (plain `tsc` fails without typegen: `LayoutProps`/`PageProps` are generated globals)
- `pnpm test` — every Vitest project once; `pnpm test:watch` to watch
- `pnpm test:unit` (unit + jsdom components), `pnpm test:content`, `pnpm test:integration` (the mentor routes, SDK mocked), `pnpm test:mentor` (the injection and answer-key suites), `pnpm test:coverage` (fails below the gates in `vitest.config.mts`)
- `pnpm test tests/unit/environment.test.ts` — one file (add `-t "<name>"` to filter by test name)
- `pnpm test:e2e` — Playwright against a production build: `pnpm build` first; the config starts `pnpm start` on port 3217. `pnpm exec playwright install chromium` once
- `pnpm bundle:check` — after a build: initial JS per page against the 200 KB budget and `scripts/bundle-baseline.json` (`--update` to accept an increase on purpose)
- `pnpm security:bundle` — after a build: fails if a secret, server-only env var name or the Anthropic SDK reached a browser file
- `pnpm perf:vitals --base http://localhost:3000` — against `pnpm start`: LCP, INP and CLS on `/`, `/cases` and `/cases/case-01`, throttled like a phone
- `pnpm format` / `pnpm format:check` — Prettier (with Tailwind class sorting)

## Tech stack

Next.js 16.3 (App Router), React 19.3, TypeScript 6 (strict, `noUncheckedIndexedAccess`, `noImplicitOverride`), Tailwind CSS 4 (CSS-first, `src/styles/`), Zod 4 (`zod/mini` in browser code), `yaml`, `@mdx-js/mdx` + `remark-gfm` + Shiki for lessons, the official Anthropic TypeScript SDK (`@anthropic-ai/sdk`) for the optional mentor, Vitest 5 projects (unit, content, integration, components/jsdom), Playwright + `@axe-core/playwright`, `tsx`, Prettier with the Tailwind plugin, `@vercel/analytics` and `@vercel/speed-insights` (loaded lazily, only on Vercel, never under Do Not Track or with the `usageCounts` setting off). No Zustand: case state will be a pure reducer held by a hook (00 §4 row 10). No database, accounts or cookies (`tests/unit/no-database.test.ts`).

- AI Mentor: `@anthropic-ai/sdk`, imported only by `src/features/mentor/anthropic-client.ts` (`import "server-only"`). Env vars: `ANTHROPIC_API_KEY` (absent means every hint is the authored text), `MENTOR_DISABLED=1` (kill switch), `MENTOR_MODEL` (override; the default is `claude-haiku-4-5`). The app works fully without any of them, and `.env.example` says so.

## Architecture

Every top-level folder under `src/` and `tests/` has a short `README.md` saying what belongs there and what it must never import. See `docs/plan/00-overview.md` §5 for where the plan puts new code.

- `src/app/` — routes only, kept thin. `(marketing)/` public pages (`/`, `/privacy`) with a footer, `(app)/` the product inside the app shell, `(dev)/` developer-only pages (a 404 in production), `search-index.json/` the build-time search index for the command palette, and `api/mentor/{hint,explain,review}/` the only code in this app that runs on a server per request (file 14; see `src/app/api/README.md`). Every route has an error and a not-found state (`not-found.tsx`, `(app)/error.tsx`, `(marketing)/error.tsx`, `global-error.tsx`, all using `ErrorState`). `/sandbox` loads its terminal through `lazy-sandbox-workspace.tsx`, so the engine isn't in the first download.
- `src/sim/` — the pure, headless, deterministic engine (vendored): `core/`, `fs/`, `net/`, `shell/`, `tools/`. The only entry point is `step(state, cmd, ctx)`; import from `@/sim` or `@/sim/types`. Adding a tool is one file plus one line in `src/sim/tools/index.ts`. `evidence/` holds the evidence model (file 02); the plan adds `src/sim/tools/forensics/` next, and the purity rules cover both automatically. Golden snapshots live in `src/sim/__fixtures__/golden/`: after an intentional output change, delete them, rerun `pnpm test`, and review the diff.
- `src/content/` — declarative data validated by Zod schemas: the sandbox workstation, the cast (`cast.ts`, speaker ids fixed), glossary, topics, terminal themes, and the banned-word list (`voice.ts`). Lessons (`lessons/*.mdx`, with `tracks.ts` and the citations in `references.ts`) are file 13; the Foundations track is written. Cases and evidence arrive in files 03 and 06.
- `src/features/<name>/` — self-contained modules with a public `index.ts` and optionally a server-only `server.ts`: `terminal` (parser, `useTerminalSession`, `Terminal`, beginner mode), `learning` (lesson loader and renderer, `<Term>`, `<Quiz>`, `<MiniTerminal>`, search index) and `mentor` (Noor: the hint ladder, "Explain this", the debrief review; only its `server.ts` reaches the Anthropic SDK). `cases` imports `mentor`, never the other way round, so the mentor's types for a case (`MentorCaseRef`, `MentorCaseSource`) are structural rather than imports.
- `src/components/` (`ui/` primitives on semantic tokens, `shell/` the app shell and command palette, `settings/` the `/settings` form), `src/lib/` (section list, "Start here" target, search, security headers, analytics, and `settings/`, the only code allowed to touch browser storage), `src/hooks/`, `src/styles/`.
- `tests/unit/`, `tests/components/` (jsdom), `tests/e2e/` (Playwright against a production build). `scripts/` holds `bundle-size.ts` and `check-client-bundle.ts`.

### Enforced import boundaries

Rules 1–4 are ESLint errors (`eslint.config.mjs`), and `tests/unit/module-boundaries.test.ts` proves they still fire:

1. `src/sim/**` may only import from `src/sim`. No `react`, `react-dom`, `next`, Node modules (`node:*`, `fs`, `net`, …) or `crypto` / `node:crypto` (use the pure hashes in `src/sim/evidence/hash/`, file 02), and no browser globals (`fetch`, `window`, `localStorage`, `crypto`, …).
2. `src/sim/**` must stay deterministic: no `Math.random()`, `Date.now()`, `performance.now()`, or argument-less `new Date()`. Inject a seeded RNG and a clock.
3. `src/content/**` may only import from `@/content` and `@/sim/types`.
4. Code outside a feature imports it only through `@/features/<name>` or `@/features/<name>/server`.
5. Only `src/lib/settings/**` may reference `localStorage`, `sessionStorage`, `indexedDB` or `document.cookie` (`tests/unit/storage-guard.test.ts`). File 05 adds `src/lib/case-storage/` for saved case runs, a deliberate divergence from Hacker Simulation (00 §4 row 9).

TypeScript `any` is a lint error.

### Browser bundle rules

- Every page's initial JS stays under 200 KB gzipped, and may not grow more than 2 KB over `scripts/bundle-baseline.json` without `--update` and a reason.
- Browser code never imports full `zod`: use `zod/mini`, or keep the schema server-side.
- The terminal and the engine load on demand (`React.lazy`); evidence JSON loads with a dynamic `import()` per case (00 §4 row 12).
- `package.json` declares `"sideEffects": ["*.css"]`. Never write a module that must run for its side effects alone.

## Working conventions

- Vendored code: read `VENDORED.md` before changing a vendored file, and update its row after. Never modify `../hacker-simulation`.
- Implement one plan file at a time; a prompt owns only the files it lists, plus one line each in the shared registration points (00 §6).
- Keep the story fictional: only cast characters and world-fact places, `.example` domains, RFC 5737 and private address ranges. Search every new name before using it.
- Never modify files unrelated to the current task.
