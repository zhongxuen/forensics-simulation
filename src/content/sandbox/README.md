# src/content/sandbox

The sandbox's practice machine (`/sandbox`): `workstation.ts` holds `WORKSTATION`, the analyst workstation `ir-ws-01` with the account `examiner` (docs/plan/99-reference.md, "New world facts"), as an engine scenario with no objectives, plus the title, one-line description and "Try this first" commands the page shows. Evidence doesn't live here: cases bring their own (docs/plan/02 and 03).

Never import here: anything except `@/content` and `@/sim/types`. Keep every name fictional and every address in a reserved range.
